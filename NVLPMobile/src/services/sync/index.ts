// Sync services will be exported from here
// Example: export { syncService } from './syncService';
// Example: export { offlineQueue } from './offlineQueue';// Placeholder export to make this a proper module
export const placeholder = src/services/sync/index.ts;
