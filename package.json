{
  "name": "nvlp",
  "version": "1.0.0",
  "private": true,
  "description": "NVLP - Virtual Envelope Budgeting System",
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "type-check": "pnpm -r type-check",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean && rimraf node_modules"
  },
  "devDependencies": {
    "@supabase/supabase-js": "^2.39.3",
    "@types/node": "^20.11.0",
    "dotenv": "^17.2.1",
    "rimraf": "^5.0.5",
    "typescript": "^5.3.3"
  },
  "packageManager": "pnpm@8.15.1",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}